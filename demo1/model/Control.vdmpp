class Control

instance variables
opCount : int;

operations
public TickUp: () ==> ()
TickUp() == (
opCount := opCount +1;
);

public Check : seq of char ==> int
Check(text) == return len text;

public Control :()==>Control
Control()==(
opCount:=0;
)

end Control
