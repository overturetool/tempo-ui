class Control

instance variables
opCount : int;

operations
public CallOp : () ==> ()
CallOp() == (
-- perform the main logic here.
opCount := opCount +1;
-- values updated. UI notified by binding.
);

public Control :()==>Control
Control()==opCount:=0;

end Control
